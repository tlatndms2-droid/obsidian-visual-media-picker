{
  "name": "visual-media-picker",
  "version": "0.1.2",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "description": "A visual asset browser for inserting vault media into Markdown and Canvas",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "esbuild": "^0.25.12",
    "obsidian": "^1.13.1",
    "tslib": "^2.8.1",
    "typescript": "^5.9.3"
  }
}
